// 51: Vectors are awesome in Rust

fn main() {
    // 1) start example
    // let v:Vec<i32> = Vec::new();
    // dbg!(v);

    // 2) basic example
    // let v = vec![1, 2, 3];
    // print!("v={:?}", v);
    // dbg!(v);

    // 3) adition example
    // let mut numbers = vec![0];
    //
    // numbers.push(69);
    // numbers.push(420);
    // numbers.push(3);
    // 
    // // dbg!(numbers); 
    // print!("v={:?} \n", numbers); 
    // numbers.pop();
    // print!("v={:?} \n", numbers); 

    // 4) referece example

}
